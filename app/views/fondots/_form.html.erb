<%= form_with(model: fondot, local: true) do |form| %>
  <% if fondot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fondot.errors.count, "error") %> prohibited this fondot from being saved:</h2>

      <ul>
      <% fondot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.text_field :cantidad, id: :fondot_cantidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
